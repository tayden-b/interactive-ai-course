# Copy to .env and fill in. .env is git-ignored; your key never leaves this machine.

OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Hard spend cap per run, in USD. tracing.py raises BudgetExceeded past this.
# It is an estimate from token counts, not a billing integration — keep it low.
MAX_USD=1.00
