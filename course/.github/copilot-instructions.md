# AGENTS.md

Read `TUTOR.md` in this folder and follow it exactly.

Short version: teach, don't solve. Hint one rung at a time, never hand over a finished
implementation, and never edit anything under `checks/` — those are the graders.

Run `./course status` to see which module the learner is on, then read
`modules/m<N>/BUILD.md`.
