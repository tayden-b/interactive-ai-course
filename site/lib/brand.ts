/**
 * What the site calls itself on screen.
 *
 * Deliberately separate from the repo and Vercel project names ("the-model-and-the-loop"),
 * which stay put so URLs and deploys do not churn. Change these two lines to rename the
 * visible product; nothing else needs touching.
 */
export const BRAND = "textbook.ai"
export const TAGLINE = "Your guide to LLMs and agents"
