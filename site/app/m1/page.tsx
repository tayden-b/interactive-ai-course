import Link from "next/link"
import { ReadingFrame, Eyebrow } from "@/components/book/reading-frame"
const sectionMeta = [["What an LLM is", "6 min"], ["Tokens: what the model reads", "7 min"], ["A probability machine", "8 min"], ["One token at a time", "6 min"], ["Temperature: choosing from the list", "7 min"], ["Where the model came from", "8 min"], ["The context window", "6 min"], ["Talking to it: prompts and conversations", "8 min"], ["What it gets wrong", "7 min"], ["The whole picture", "5 min"], ["Project: your first call", "25 min"]] as const

export default function ModuleOne() { return <ReadingFrame lesson={1} title="What is an LLM?"><article><p className="font-mono text-[10px] text-muted-foreground">Modules / 01</p><h1 className="mt-5 font-display text-6xl">What is an LLM?</h1><p className="mt-6 max-w-2xl text-base leading-7 text-muted-foreground">Everything else in this book sits on top of a language model, so this module is about the model itself: what it reads, what it produces, where it came from, and what it gets wrong. No installation, no code, no math beyond a percentage. About ninety minutes.</p><section className="mt-12 border-t border-border pt-6"><Eyebrow>What you'll be able to do</Eyebrow><ul className="mt-4 list-disc space-y-2 pl-5 text-sm leading-6 text-muted-foreground"><li>Explain what a language model does in one sentence.</li><li>Read a next-word probability chart and say what temperature does to it.</li><li>Say why a model can be confidently wrong, and what that means for everything built on it.</li></ul></section><section className="mt-12 border border-border p-5"><Eyebrow>Project</Eyebrow><h2 className="mt-3 font-display text-3xl">Your first call</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">At the end of this module you set up your lab and make one real call to a model — and the same probability chart you read in Section 3 shows your prompt and your numbers.</p><p className="mt-5 font-mono text-[10px] text-muted-foreground">Section 11</p></section><section className="mt-12"><Eyebrow>Sections</Eyebrow><div className="mt-4 border-t border-border">{sectionMeta.map(([title, meta], i) => <Link key={title} href={`/m1/s${i + 1}`} className="grid grid-cols-[40px_1fr_auto] items-center gap-3 border-b border-border py-4 text-sm hover:bg-secondary"><span className="font-mono text-[10px] text-muted-foreground">{String(i + 1).padStart(2, "0")}</span><span>{title}</span><span className="font-mono text-[10px] text-muted-foreground">{i === 0 ? "Start · " : ""}~{meta}</span></Link>)}</div></section></article></ReadingFrame> }
