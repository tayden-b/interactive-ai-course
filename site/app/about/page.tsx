import { ReadingFrame, LessonHeader, Prose } from "@/components/book/reading-frame"
export default function About() { return <ReadingFrame module={0} lesson={0} title="About"><LessonHeader eyebrow="About" meta="A short note">Why this book exists.</LessonHeader><div className="mt-10"><Prose><p>This book exists to make the invisible parts of AI agents concrete: the requests, loops, context, traces, and small decisions that turn a model into a system you can reason about.</p></Prose><h2 className="mt-16 font-display text-3xl">What you&apos;ll touch</h2><div className="mt-6 grid grid-cols-2 gap-x-8">{["OpenAI API", "Anthropic API", "Python", "OpenTelemetry", "JSON Schema", "tiktoken", "GitHub", "Markdown"].map(item => <div key={item} className="border-b border-border py-3 font-mono text-xs">{item}</div>)}</div></div></ReadingFrame> }
