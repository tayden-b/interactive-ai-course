import Link from "next/link"
import { AnimatedSphere } from "@/components/landing/animated-sphere"
import { ReadingFrame, Eyebrow } from "@/components/book/reading-frame"
import { courseModules } from "@/components/book/course-data"

export default function Home() { return <ReadingFrame><main><section className="grid min-h-[calc(100vh-90px)] items-center gap-10 border-b border-border py-14 md:grid-cols-2 md:py-20"><div><Eyebrow>A visual, hands-on guide to AI — from the model up</Eyebrow><h1 className="mt-5 max-w-xl text-balance font-display text-6xl leading-[.95] tracking-tight md:text-8xl">Understand AI by <span className="text-muted-foreground">building</span> with it</h1><p className="mt-7 max-w-xl text-base leading-7 text-muted-foreground">Eight modules. Each one explains a building block in plain language, with a diagram for every idea, and ends with something you build.</p><Link href="/m/1" className="mt-9 inline-flex bg-primary px-5 py-3 text-sm text-primary-foreground">Start with Module 1</Link></div><div className="relative -mx-6 h-[520px] overflow-visible md:-mx-16 md:h-[700px]"><AnimatedSphere /></div></section><section className="border-b border-border py-16"><Eyebrow>The modules</Eyebrow><div className="mt-6 grid grid-cols-1 gap-px border border-border bg-border sm:grid-cols-2 lg:grid-cols-4">{courseModules.map((item, i) => <Link key={item.title} href={`/m/${i + 1}`} className="min-h-56 bg-background p-5 transition-colors hover:bg-secondary"><span className="font-mono text-[10px]">{String(i + 1).padStart(2, "0")}</span><h2 className="mt-9 font-display text-2xl">{item.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{item.description}</p><p className="mt-4 font-mono text-[10px] text-muted-foreground">PROJECT — {item.project}</p><span className="mt-5 block font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Open</span></Link>)}</div></section></main></ReadingFrame> }
