"use client"

import Link from "next/link"
import { useState } from "react"
import { Check, Eyebrow, Figure, LessonHeader, Prose } from "./reading-frame"
import { Fig07 } from "@/components/figures/course-figures"

type Lesson = { node?: React.ReactNode; title: string; time: string; line: string; figure: string; caption: string; library?: string; paragraphs: string[]; idea: string; check?: { prompt: string; options: string[]; correct: number; feedback: string[] }; deeper: [string, string]; project?: boolean; bullets?: string[]; end?: string }

const lessons: Lesson[] = [
 { node: <Fig07 />, title: "What you’re building", time: "~8 min", line: "A research desk: question in, sourced brief out, with every agent’s work visible — built from the patterns you already have.", figure: `public page → input gate → orchestrator → researcher A | researcher B | researcher C → checker → writer → output gate → brief\n                         └ every span recorded → live trace   memory ↔ orchestrator   budget`, caption: "FIGURE 8.1.1 — The Desk. Every box is a module you’ve done.", library: "A static SVG architecture diagram with hover-to-highlight per component.", paragraphs: ["Most portfolio projects are a chatbot with a wrapper. This is a system: it plans, delegates, verifies, remembers, refuses, and reports — and it shows its work.", "Read the diagram with Module 7 in mind. It’s orchestrator–workers with a critic in the loop, inside a workflow with gates. Nothing here is new; the newness is the integration."], idea: "The Desk is a system that plans, delegates, verifies, remembers, refuses, and reports — and shows its work.", deeper: ["Anthropic, ‘How we built our multi-agent research system.’", "https://www.anthropic.com/engineering/built-multi-agent-research-system"], project: true, bullets: ["An orchestrator that turns a question into a research plan (Module 7).", "Three researcher agents with search, fetch, and read tools, run in parallel, each in its own window (Modules 3, 4, 7).", "A checker that verifies each claim against its source (Modules 5, 6).", "A writer that produces a sourced brief in a fixed format (Module 2).", "Memory across runs: what’s been researched before, and user preferences (Module 4).", "Guardrails at the input, action, and output gates, with caps and a per-run budget (Module 6).", "An eval suite in CI with golden questions (Module 5).", "Full tracing, rendered live on a public page as a waterfall (Module 1’s chart, grown up).", "Deployed, with a URL you can send to someone."], end: "A working, deployed multi-agent system with a public demo, a trace you can narrate, and a number that says how well it works." },
 { title: "The orchestrator", time: "~8 min", line: "The orchestrator turns a question into a small plan of independent research tasks, delegates them, and synthesizes — and it never reads a source itself.", figure: `QUESTION → ORCHESTRATOR\n              plan: [task + goal + allowed tools]\n                 ↙          ↓          ↘\n           researcher A  researcher B  researcher C\n                 ↘          ↓          ↙\n              summaries → checker → writer`, caption: "FIGURE 8.2.1 — The orchestrator plans and synthesizes. It doesn’t research.", paragraphs: ["The orchestrator is the only agent that sees the whole task, so the discipline is keeping its window small. It reads the question, consults memory for what’s already known, and produces a plan: two to four research tasks that can run independently, each with a goal and the tools it may use. That plan is structured output (Module 2), validated before anything runs.", "It then waits. When the workers’ summaries come back — a few hundred tokens each — it decides whether the question is answered or a second round is needed, and hands the results to the checker and writer. It never fetches a page. If it did, one large document would crowd out the plan.", "The two things to eval here: is the plan a good decomposition (a judge with a rubric), and does the orchestrator stop after one or two rounds (a trace assertion on step count)."], idea: "The orchestrator’s power is a small window; it plans, delegates, and synthesizes, nothing else.", check: { prompt: "The orchestrator’s window should contain…", options: ["Every source the workers found.", "The question, the plan, memory, and the workers’ summaries.", "Only the question."], correct: 1, feedback: ["That defeats the architecture.", "Yes.", "It needs the plan and results to synthesize."] }, deeper: ["Anthropic, ‘Building effective agents’ — orchestrator-workers.", "https://www.anthropic.com/research/building-effective-agents"] },
 { title: "The specialists", time: "~8 min", line: "Each researcher takes one task, uses its tools in a short loop, and returns a compact summary with sources; the checker verifies each claim; the writer formats.", figure: `RESEARCHER          CHECKER             WRITER\nsearch → fetch → read  claim ↔ excerpt      headline\n       → summary + 3 sources  score 0–2      findings + citations`, caption: "FIGURE 8.3.1 — Narrow agents with narrow windows.", paragraphs: ["Three specialist roles, each deliberately narrow.", "The researcher is the Module 3 agent with a job description: one task, three tools, a cap of eight steps, and a required return shape — a summary under 300 tokens plus the sources it used. It runs in parallel with its siblings and never sees their windows.", "The checker is Module 5’s judge moved into the workflow (Module 7’s critic): for every claim in a researcher’s summary, find the supporting excerpt in the cited source and score it. Unsupported claims are dropped, not softened. This is where the Desk earns trust.", "The writer is a workflow step, not an agent: given checked claims and sources, produce the brief in a fixed format. Structured output with a schema, validated, retried. No tools, no decisions, no surprises."], idea: "Researchers gather, the checker verifies, the writer formats — three narrow jobs, three small windows.", check: { prompt: "A researcher’s summary contains a claim the checker can’t find in the source. What happens?", options: ["Keep it with a caveat.", "Drop it.", "Ask the researcher again."], correct: 1, feedback: ["Unsupported claims are dropped.", "Yes.", "Possible in a second round — but never pass it through."] }, deeper: ["Anthropic, ‘Building effective agents’ — evaluator-optimizer, and the ACI section on tool design.", "https://www.anthropic.com/research/building-effective-agents"] },
 { title: "The harness", time: "~8 min", line: "The harness is the code around all the agents — state, retries, timeouts, budgets, and the ability to pause and resume — and it’s what makes the system production-grade.", figure: `planned → researching (3 parallel) → checking → writing → done\n     ↘ paused (approval)                 ↘ stopped (cap hit)\nstate stored · retry count · elapsed time · tokens spent`, caption: "FIGURE 8.4.1 — The run as a state you can save, resume, and inspect.", paragraphs: ["Agents are the interesting part; the harness is the part that makes them survivable.", "State: every run has a record — the plan, each task’s status, the results so far — stored outside any agent’s window, so a crash mid-run doesn’t lose the work and a run can be resumed. Retries: a failed tool call or a malformed model output gets retried with the error (Module 2), up to a limit. Timeouts: each worker has one; the run has one. Budgets: tokens and dollars per run, enforced, with a graceful stop that returns what was found. Pauses: an approval step before any action that sends or spends, implemented as a state the run can sit in until a person answers.", "None of this is AI. All of it is why the system can be trusted with a URL."], idea: "The harness owns state, retries, limits, and pauses; agents are stateless inside it.", check: { prompt: "A worker crashes halfway through a run. What should happen?", options: ["The whole run fails.", "The run resumes from saved state, retrying the worker.", "The orchestrator starts over."], correct: 1, feedback: ["Only if you kept state inside the agent.", "Yes.", "Wasteful — the other workers’ results are saved."] }, deeper: ["12-factor agents — Factors 5, 6, 8: unify state, launch/pause/resume, own your control flow.", "https://github.com/humanlayer/12-factor-agents"] },
 { title: "Memory across runs", time: "~6 min", line: "The Desk remembers what it has researched and what each user prefers, so the second question is better than the first.", figure: `MEMORY STORE\nresearch memory: questions → checked claims + sources + dates\nuser memory: depth · format · preferred sources\n                 ↕ read at plan time · write at run end`, caption: "FIGURE 8.5.1 — What the Desk writes down for next time.", paragraphs: ["Module 4 gave one agent notes. The Desk gives the system two kinds.", "Research memory: every completed run stores its question, its checked claims, and its sources. When a new question arrives, the orchestrator retrieves related past runs and includes their summaries in the plan’s context — so a follow-up question doesn’t re-research what’s already known, and a contradiction with last month’s brief is visible.", "User memory: preferences that should persist — how long a brief should be, which sources to prefer or avoid, what format. Written when a user says so, read at plan time.", "The rule from Module 4 holds: notes are short, structured, and maintained, and only the relevant ones enter the window."], idea: "Two memories, both outside the window: what’s been researched and what the user wants.", check: { prompt: "A user asks a follow-up to last week’s question. What should the orchestrator see?", options: ["Nothing from last week.", "Last week’s checked summary, retrieved into the plan context.", "Last week’s full sources."], correct: 1, feedback: ["Then it re-researches.", "Yes.", "Too large; the summary is enough."] }, deeper: ["Grootendorst, ‘A Visual Guide to LLM Agents,’ Memory.", "https://newsletter.maartengrootendorst.com/p/a-visual-guide-to-llm-agents"] },
 { title: "Observability", time: "~8 min", line: "Every model call and tool call in a run is a span with tokens, cost, and timing; rendered as a tree with a waterfall, it’s how you debug, bill, and demo the Desk.", figure: `run ├─ orchestrator: plan      [██████] 2.1s\n    ├─ researcher A ─ model [██] ─ search [███]\n    ├─ researcher B ─ model [██] ─ fetch  [████]\n    ├─ researcher C ─ model [██] ─ read   [██]\n    ├─ checker                         [███]\n    └─ writer                          [██]   tokens · cost`, caption: "FIGURE 8.6.1 — One run, fully visible. This is the page people will stare at.", library: "agent-prism, OpenTelemetry GenAI conventions.", paragraphs: ["You’ve been writing traces since Module 1. The Desk is where they become the product’s face.", "Every span — a model call, a tool call, a whole agent’s run — is recorded with its parent, its timing, its tokens, and its cost, in a standard shape (OpenTelemetry’s conventions for AI), so any trace viewer can render it. The view that works is a tree with an inline waterfall: nesting shows who called whom, the bars show when and how long, the numbers show what it cost.", "Three uses. Debugging: a bad brief is traced to the worker, the tool call, and the page that misled it. Billing: cost per run, per component, over time — so you know the checker is a tenth of the bill and the researchers are the rest. Demoing: the live trace on the public page is the most convincing thing a visitor will see, because it shows the work instead of asserting it."], idea: "The trace is the system’s memory of what it did; render it, and debugging, billing, and demoing are the same screen.", check: { prompt: "A brief contains a wrong figure. Where do you look first?", options: ["The writer’s prompt.", "The trace: which researcher, which tool call, which source.", "The model’s settings."], correct: 1, feedback: ["The writer only formats checked claims.", "Yes.", "Not until the trace points there."] }, deeper: ["Langfuse’s trace UI documentation — tree and timeline views of agent runs.", "https://langfuse.com/docs/observability/features/trace-ui"] },
 { title: "Evals and guardrails in CI", time: "~7 min", line: "Golden questions with checked answers run on every change; gates at input, action, and output run on every request; both are reported, neither is optional.", figure: `CI EVALS: plan quality · researcher recall · checker precision · brief format\nINPUT GATE ───── ACTION GATE ───── OUTPUT GATE\nblocked: 12       approvals: 3       rejected: 1`, caption: "FIGURE 8.7.1 — Measured on every change; guarded on every request.", paragraphs: ["Two disciplines from Modules 5 and 6, applied to a system instead of one agent.", "Evals: twenty golden questions with checked answers and ‘what matters’ notes. Graders per component — a judge for plan quality, a code check that researchers returned sources, a precision check on the checker (no unsupported claim survives), exact-match on the brief’s format. Run in CI; the number is recorded with every commit; a drop blocks the merge.", "Guardrails: the input gate rejects off-topic or injection-shaped requests and enforces length. The action gate allow-lists tools per role and requires approval for anything that sends or spends. The output gate validates the brief’s shape and asserts every claim has a source. Caps and budgets on every worker and every run. Each block is recorded in the trace, so ‘how often do guardrails fire’ is a number too."], idea: "Evals tell you it works; guardrails make sure it fails safely; both produce numbers you report.", check: { prompt: "The checker starts letting unsupported claims through. What catches it?", options: ["A user complaint.", "The eval on checker precision, in CI.", "The output gate."], correct: 1, feedback: ["Eventually. Too late.", "Yes.", "It asserts sources exist, not that they support the claim — that’s the checker’s eval."] }, deeper: ["Hamel Husain, ‘Your AI product needs evals.’", "https://hamel.dev/blog/posts/evals/"] },
 { title: "Deploy and show it", time: "~8 min", line: "Put the Desk behind a URL with a page where anyone can ask a question and watch the agents work — then learn to narrate the trace in two minutes.", figure: `ASK THE DESK [________________________________]\n\nLIVE TRACE WATERFALL\nrun → plan → researchers in parallel → check → write\n\nFINISHED BRIEF                                      SHARE`, caption: "FIGURE 8.8.1 — The demo is the trace.", paragraphs: ["The interview version is two minutes: here’s the question, here’s the plan the orchestrator made, here are the three researchers running at once — watch the windows stay small — here’s a claim the checker dropped and why, here’s the brief, here’s what it cost, and here’s the eval that would catch it if the checker got lazy.", "Nothing in that story is a claim. All of it is on the screen.", "Build steps arriving with the lab."], idea: "You built a system that plans, delegates, verifies, remembers, refuses, and reports — and shows its work.", deeper: ["Anthropic, ‘How we built our multi-agent research system.’", "https://www.anthropic.com/engineering/built-multi-agent-research-system"], project: true, bullets: ["A deployed service (the harness as an API) with the public page.", "The live trace view, streaming spans as they’re created.", "A rate limit and a per-visitor budget, so the demo can’t be run up.", "A ‘how it’s built’ section on the page with the architecture diagram and the eval numbers.", "Your build page: the Desk, its trace, its pass rate, and your write-up."], end: "A URL you can send to a hiring manager, a trace you can narrate, and numbers you can defend." },
]

function MiniCheck({ check }: { check: NonNullable<Lesson["check"]> }) { const [selected, setSelected] = useState<number | null>(null); return <Check><span className="block">{check.prompt}</span><div className="mt-3 grid gap-2">{check.options.map((option, i) => <button key={option} onClick={() => setSelected(i)} className={`border px-3 py-3 text-left text-sm ${selected === i ? "border-foreground" : "border-border"}`}><span className="mr-2 font-mono text-[10px]">{String.fromCharCode(65 + i)}</span>{option}</button>)}</div>{selected !== null && <span className="mt-4 block border-t border-border pt-4 text-muted-foreground">{check.feedback[selected]}</span>}</Check> }

export function ModuleEightHome() { return <article><Eyebrow>Modules / 08</Eyebrow><h1 className="mt-5 font-display text-6xl">Capstone: The Desk</h1><p className="mt-6 max-w-2xl text-base leading-7 text-muted-foreground">Everything in this book, in one system you can show. The Desk is a production multi-agent research service: someone submits a question, an orchestrator plans, specialist agents research in parallel with real tools, a checker verifies every claim against its source, a writer produces the brief, and the whole run is visible as a live trace — with memory across runs, evals and guardrails in CI, a budget, and a public page anyone can use. It’s the portfolio piece. It’s also the proof that the seven modules before it were one idea.</p><section className="mt-10 border-t border-border pt-6"><Eyebrow>What you’ll be able to do</Eyebrow><ul className="mt-4 list-disc space-y-2 pl-5 text-sm leading-6 text-muted-foreground"><li>Design a multi-agent system from the context windows outward.</li><li>Explain every span in a production trace, and what it cost.</li><li>Deploy an agent service with evals, guardrails, and observability, and talk about it in an interview.</li></ul></section><section className="mt-12 border border-border p-5"><Eyebrow>Project · The Desk</Eyebrow><h2 className="mt-3 font-display text-3xl">The Desk</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Eight sections, one system. At the end it’s deployed, traced, tested, and public.</p><Link href="/m/8/s/1" className="mt-5 inline-block font-mono text-[10px] uppercase tracking-[.16em] underline underline-offset-4">Start capstone →</Link></section></article> }

export function ModuleEightLesson({ section = 1 }: { section?: number }) { const item = lessons[Math.max(0, Math.min(7, section - 1))]; return <article><LessonHeader eyebrow={`Module 08 · Section ${section} of 8`} meta={`${item.time}${item.project ? " · Project" : ""}`}>{item.title}</LessonHeader><div className="mt-10 border border-border px-4 py-3"><Eyebrow>In one line</Eyebrow><p className="mt-2 text-lg leading-7">{item.line}</p></div><Figure caption={item.caption} library={item.node ? "" : item.library}>{item.node ?? <pre className="whitespace-pre-wrap p-5 font-mono text-xs leading-6 text-muted-foreground">{item.figure}</pre>}</Figure>{item.project && <section className="border border-border p-5"><Eyebrow>What you’ll build</Eyebrow><ul className="mt-3 list-disc space-y-2 pl-5 text-sm leading-6 text-muted-foreground">{item.bullets?.map((bullet) => <li key={bullet}>{bullet}</li>)}</ul><p className="mt-5 border-t border-border pt-4 text-sm leading-6 text-muted-foreground"><span className="font-mono text-[10px] uppercase tracking-[.16em]">What you’ll have at the end — </span>{item.end}</p></section>}<Prose>{item.paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</Prose><aside className="mt-10 border border-border p-5"><Eyebrow>Key idea</Eyebrow><p className="mt-2 text-sm leading-6">{item.idea}</p></aside>{item.check && <MiniCheck check={item.check} />}<details className="mt-8 border-y border-border py-4"><summary className="cursor-pointer font-mono text-[10px] uppercase tracking-[.16em] text-muted-foreground">Go deeper</summary><a href={item.deeper[1]} target="_blank" rel="noreferrer" className="mt-4 block text-sm underline underline-offset-4">{item.deeper[0]}</a><p className="mt-2 break-all font-mono text-xs text-muted-foreground">{item.deeper[1]}</p></details><div className="mt-12 flex items-center justify-between border-t border-border pt-6 font-mono text-[10px]"><Link href={section > 1 ? `/m/8/s/${section - 1}` : "/m/8"}>← Previous</Link><Link className="text-foreground" href={section < 8 ? `/m/8/s/${section + 1}` : "/m/8"}>{section < 8 ? "Next →" : "Back to module →"}</Link></div></article> }
