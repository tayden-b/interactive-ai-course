# Set this once a custom domain is attached, so absolute URLs and the link-preview image
# resolve correctly. Without it the build falls back to Vercel's deployment URL.
NEXT_PUBLIC_SITE_URL=https://themodelandtheloop.com
